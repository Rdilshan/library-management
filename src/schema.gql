# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Admin {
  id: Int!
  name: String!
  email: String!
  username: String!
  password: String!
  role: Role!
}

enum Role {
  Super
  manager
}

type Book {
  id: Int!
  title: String!
  Author: String!
  year: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Member {
  id: Int!
  email: String!
  name: String!
}

type BorrowBook {
  id: Int!
  bookId: Float!
  userId: Float!
  bringDate: DateTime!
  endDate: DateTime!
  finished: Visible!
}

enum Visible {
  yes
  no
}

type Query {
  getAdmins: [Admin!]!
  getAdmin(id: Int!): Admin!
  LoginAdmin(email: String!, password: String!): String!
  getBooks: [Book!]!
  getBook(id: Int!): Book!
  getMembers: [Member!]!
  getMember(id: Int!): Member!
  getBorrowBooks: [BorrowBook!]!
  getborrowBook(id: Int!): BorrowBook!
  BringBookset: [BorrowBook!]!
  BorrowBookset: [BorrowBook!]!
}

type Mutation {
  createAdmin(name: String!, email: String!, username: String!, password: String!): Admin!
  UpdateAdmin(id: Int!, name: String!, email: String!, username: String!): Admin!
  DeleteAdmin(id: Int!): Admin!
  createBook(title: String!, Author: String!, year: DateTime!): Book!
  UpdateBook(id: Int!, title: String!, Author: String!, year: DateTime!): Book!
  DeleteBook(id: Int!): Book!
  createMember(email: String!, name: String!): Member!
  UpdateMember(id: Int!, email: String!, name: String!): Member!
  DeleteMember(id: Int!): Member!
  createBorrowBook(bookId: Float!, userId: Float!, bringDate: DateTime!, endDate: DateTime!): BorrowBook!
  UpdateBorrowBook(id: Int!, bookId: Float!, userId: Float!, bringDate: DateTime!, endDate: DateTime!): BorrowBook!
  DeleteBorrowBook(id: Int!): BorrowBook!
  updateBringBook(id: Int!): BorrowBook!
}